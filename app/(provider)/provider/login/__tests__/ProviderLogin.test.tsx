import { describe, it } from "@jest/globals";
import { render } from "@testing-library/react";
import ProviderLogin from "@/app/(provider)/provider/login/page";

describe("ProviderLogin", () => {
  it("should ", () => {
    render(<ProviderLogin />);
  });
});
